apiVersion: v1
kind: Service
metadata:
  name: cyberpeaks-nl-service
  namespace: prd
spec:
  type: ClusterIP
  selector:
    app: cyberpeaks-nl-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
