# Self signed Issuer for testing purposes
# apiVersion: cert-manager.io/v1
# kind: Issuer
# metadata:
#   name: letsencrypt-issuer
#   namespace: prd
# spec:
#   selfSigned: {}

# Let's Encrypt
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: letsencrypt-issuer
  namespace: prd
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: letsencrypt.font909@passmail.net
    privateKeySecretRef:
      name: letsencrypt-issuer-secret
    solvers:
    - http01:
        ingress:
          ingressClassName: nginx
